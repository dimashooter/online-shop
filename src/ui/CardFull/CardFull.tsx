import { useRouter } from 'next/router'
import React from 'react'

const CardFull = () => {

  const params = useRouter()

  // const uniqueCourse = api.courses.getOne.useQuery({ courseId: course.id })
  return (
    <div>

    </div>
  )
}

export default CardFull
